/* ------------------------------------------------------------------
   products.js — витрина продуктов и страницы продуктов.
   Раздел существует только в русской версии сайта.
   ------------------------------------------------------------------ */

/* ===== Ссылки на оплату ===========================================
   Пока строка пустая, кнопка «Купить» ведёт на форму обратной связи
   и подставляет название продукта в сообщение — заявка приходит на
   почту. Когда подключите эквайринг, впишите сюда адреса страниц
   оплаты, и кнопки начнут вести прямо туда.
   ================================================================= */
var PAYMENT_LINKS = {
  spiktera:     '',   // Курс «Спиктера» — 15 000 ₽
  constructor:  '',   // Конструктор готовых решений — 35 000 ₽
  calculator:   '',   // Калькулятор силы бренда — 1 990 ₽
  subscription: ''    // Подписка комьюнити «Сборка» — 1 000 ₽ / месяц
};

(function () {
  'use strict';

  // Название продукта подставляется в письмо, чтобы сразу было понятно,
  // о чём заявка.
  var TITLES = {
    spiktera:     'Курс «Спиктера»',
    constructor:  'Конструктор «Готовые решения для туристических проектов»',
    calculator:   'Калькулятор силы бренда',
    subscription: 'Подписка комьюнити «Сборка»'
  };

  function apply() {
    [].forEach.call(document.querySelectorAll('[data-buy]'), function (a) {
      var key = a.getAttribute('data-buy');
      var paid = PAYMENT_LINKS[key];

      if (paid) {
        a.href = paid;
        a.target = '_blank';
        a.rel = 'noopener noreferrer';
        return;
      }
      // Запасной путь: форма на главной с подставленным продуктом
      a.href = '/?product=' + encodeURIComponent(TITLES[key] || key) + '#contact';
      a.removeAttribute('target');
    });
  }

  document.addEventListener('DOMContentLoaded', function () {
    apply();
    if (window.MOTION) MOTION.reveal();
  });
})();
